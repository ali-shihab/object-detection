%==========================================================================================
% COMP0248 Coursework 1 (LSA) -- technical report
% 6 pages MAXIMUM including references (LSA p13, p15). IEEE conference template, unaltered.
%
% HOW THIS FILE WORKS
%   * Every number and every table under report/generated/ is GENERATED from the experiment
%     JSONs by  tools/make_report_tables.py.  Re-run it after any new result and the tables
%     update themselves.  Do not type a number into the prose: use the \newcommand macros in
%     generated/macros.tex (e.g. \EthreePhoneMacroF) so the text can never disagree with a table.
%   * Every figure under report/figures/ is generated by  src/visualise.py.
%   * The prose is YOURS to write.  Each section below carries a comment block listing the
%     facts that section needs to state, with a pointer to where each one is established.
%     Nothing in this file is drafted text -- the section bodies are empty on purpose.
%==========================================================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{algorithmic}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{subcaption}
\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\graphicspath{{figures/}}
\input{generated/macros}

\begin{document}

\title{Cross-Camera Hand Gesture Detection, Segmentation and Classification from RGB}

\author{\IEEEauthorblockN{Ali Shihab}
\IEEEauthorblockA{\textit{MSc Robotics and Artificial Intelligence} \\
\textit{University College London}\\
London, United Kingdom \\
18006111}
}

\maketitle

\begin{abstract}
% ~150 words. State: the task (one RGB image -> box + mask + gesture + confidence); that the
% model is a single multi-task network trained from scratch; the headline RealSense result
% (\EoneRsMacroF, \EoneRsHandIoU, \EoneRsDetAcc); the size of the zero-shot cross-camera drop
% (\EtwoPhoneMacroF); the method you introduced and what it recovers (\GainMacroF).
\end{abstract}

\begin{IEEEkeywords}
hand gesture recognition, multi-task learning, semantic segmentation, object detection,
domain generalisation, cross-camera transfer
\end{IEEEkeywords}

%==========================================================================================
\section{Introduction}
%------------------------------------------------------------------------------------------
% Facts available to you, with sources:
%  * Problem statement: LSA brief p3-p5 -- one RGB image, four outputs, ten gesture classes.
%  * Relevance to robotics: LSA brief p4 (HRI command interface; hands occlude tools in
%    manipulation; real-world variability in background, lighting and skin tone).
%  * The specific gap this coursework targets: a model trained on one camera (RealSense D455)
%    and deployed on another (a phone).  Why that matters in robotics: the sensor on the robot
%    is rarely the sensor the dataset was collected with.
%  * Your contributions, as a short list.  See docs/02_DESIGN.md s3, s7 for what they are.
%------------------------------------------------------------------------------------------

%==========================================================================================
\section{Dataset}
%------------------------------------------------------------------------------------------
% Everything here is verified in docs/01_DATA.md.  Numbers you may state:
%  * RealSense train/val: 20{,}550 frames, 30 contributors, 1{,}500 clips, 10 classes exactly
%    balanced at 2{,}055 frames each (01_DATA s5.1).
%  * Only 14.1\% of training frames carry a hand mask (2 keyframes per clip), while the test
%    release annotates all 15 frames of every clip (01_DATA s5.1).  This asymmetry drives a
%    design decision in Section~\ref{sec:method}.
%  * RealSense test: 3{,}450 frames, 230 clips, 5 unidentified holdout subjects, 100\% annotated.
%  * Hand geometry: median hand area 2.75\% of the image; median box 115x151 px at 640x480;
%    median aspect 0.76; centres near (0.448, 0.489); 1.30\% of frames border-touching
%    (01_DATA s2).
%  * Data defects you found and handled: a contributor lost to a nested wrapper directory, a
%    byte-identical duplicate submission, three partial submissions (01_DATA s5.2).  Worth ~3
%    sentences: it shows the dataset was audited rather than trusted.
%  * Smartphone set: 10 gestures x 2 clips x 3 frames = 60 images, capture protocol in
%    smartphone_dataset/RECORDING_GUIDE.md, masks produced by the pipeline in
%    tools/annotate_smartphone.py and manually verified.
%  * The split is contributor-wise, never frame-wise, because consecutive frames of a clip have
%    a median mask IoU of 0.649 (01_DATA s2).  This is worth one sentence and it is the kind of
%    thing markers look for.
%------------------------------------------------------------------------------------------

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{augmentation_grid}
  \caption{One RealSense frame rendered through each photometric policy (top) and through five
  independent draws of the proposed randomisation (bottom).}
  \label{fig:aug}
\end{figure}

%==========================================================================================
\section{Methodology}\label{sec:method}
%------------------------------------------------------------------------------------------
% Source: docs/02_DESIGN.md s2-s4, s7.  Cover, in this order:
%  * Architecture (s3): shared residual encoder at strides 2-32 (24/48/96/192/320 channels,
%    2/3/4/3 blocks), U-Net segmentation decoder with lateral skips, centre-point detection head
%    at stride 4, classification head with mask-attended pooling.  10.0M parameters, trained from
%    random initialisation.  Say explicitly that no torchvision model, no torchvision.ops and no
%    pretrained weights are used, and that IoU/GIoU/peak suppression are implemented in the repo.
%  * Why a centre-point head rather than regressing four numbers (s3.3).
%  * Mask-attended pooling and the hypothesis it encodes (s3.4) -- and note that Section
%    \ref{sec:results} tests that hypothesis rather than asserting it.
%  * Loss composition (s4): the six terms and their weights; why GIoU is included; why fixed
%    weights are the default and uncertainty weighting is an ablation.
%  * The sparse-mask rule (s4.1): classification over the whole batch, detection and
%    segmentation over the annotated subset only.  This is the direct consequence of the 14.1\%
%    annotation density and is worth stating as a design decision, not a detail.
%  * The proposed cross-camera method (s7): Camera-Pipeline Randomisation.  Give the stage table
%    or a compressed version of it, and the bidirectionality argument (phone images are sharper
%    and more saturated than RealSense frames, so a policy that only degrades covers the wrong
%    direction).
%  * The target-free validation protocol (s7.1) and why it exists.  State plainly that the
%    smartphone set was evaluated once, at the end.
%------------------------------------------------------------------------------------------

%==========================================================================================
\section{Experimental setup}
%------------------------------------------------------------------------------------------
% Source: docs/02_DESIGN.md s10, docs/03_IMPLEMENTATION.md s2.
%  * Input 384x288 (4:3 preserved, divisible by 32); packed dataset at 512x384.
%  * AdamW, lr 3e-4, cosine schedule with 3-epoch warmup, weight decay 0.05, batch 48, bf16
%    autocast, gradient clip 1.0, 60 epochs, EMA of weights for evaluation.
%  * Hardware: one NVIDIA RTX 4070 Ti SUPER (16 GB), ~50 s/epoch.
%  * Metric definitions (02_DESIGN s9), including that hand IoU is reported separately because
%    background IoU is ~0.97 by construction and flatters the mean.
%  * Bootstrap CIs resample clips, not frames, and why.
%  * Every configuration in this study shares one config file and differs only in the fields
%    named in the ablation tables.
%------------------------------------------------------------------------------------------

%==========================================================================================
\section{Results}\label{sec:results}
%------------------------------------------------------------------------------------------
% All tables and figures below are generated.  Your prose should walk the reader through:
%  1. The RealSense benchmark (Experiment 1) against the classical baseline -- Table~\ref{tab:main}.
%  2. The zero-shot cross-camera drop (Experiment 2) -- Table~\ref{tab:main}, Fig.~\ref{fig:gap}.
%     Which task degrades most, and what that says about where the shift bites.
%  3. The recovery from the proposed method (Experiment 3) -- Table~\ref{tab:gain}.
%  4. What the ablations attribute the gain to -- Table~\ref{tab:ablation}.  Be explicit about
%     which deltas exceed the seed spread and which do not.
%  5. Qualitative behaviour -- Fig.~\ref{fig:qual}, Fig.~\ref{fig:cm}.
%------------------------------------------------------------------------------------------

\input{generated/tbl_main}
\input{generated/tbl_gain}
\input{generated/tbl_ablation}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{domain_gap}
  \caption{The same metrics across evaluation domains. Error bars are bootstrap 95\% intervals
  over clips.}
  \label{fig:gap}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{cm_e3_phone}
  \caption{Row-normalised confusion matrix on the smartphone test set.}
  \label{fig:cm}
\end{figure}

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{qualitative}
  \caption{Predicted masks (filled) and boxes (solid) against ground truth (dashed). Panels with
  a heavy border are misclassifications.}
  \label{fig:qual}
\end{figure*}

\input{generated/tbl_perclass}

%==========================================================================================
\section{Discussion}
%------------------------------------------------------------------------------------------
% Things worth discussing, all of which you have evidence for:
%  * Which of the three tasks is most and least robust to the camera change, and why that is
%    consistent (or not) with the mask-attended-pooling hypothesis.
%  * Calibration: does confidence stay meaningful across the domain shift?  (ECE is in every
%    result JSON and Fig. rel_*.pdf is generated.)
%  * Limitations, honestly: 60 smartphone images is a small test set and the intervals show it;
%    one photographer and one phone; the pseudo-target shift overlaps CPR in two of its four
%    operations (02_DESIGN s7 corrections); the model is trained from scratch, so the absolute
%    numbers are below what a pretrained backbone would give -- and say why that choice was made.
%  * What the classical baseline tells you about how much of the task is skin colour.
%------------------------------------------------------------------------------------------

%==========================================================================================
\section{Conclusion}
%------------------------------------------------------------------------------------------
% Lessons learned and future work.  Candidates for future work that follow from your own
% results rather than from a generic list: test-time adaptation (excluded here because the brief
% forbids using target data in training -- 02_DESIGN s7); learned rather than hand-specified
% imaging randomisation; more than one phone; propagating masks through clips to lift the 14.1\%
% annotation density.
%------------------------------------------------------------------------------------------

\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
